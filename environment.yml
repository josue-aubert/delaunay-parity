name: delaunay-parity
channels:
  - conda-forge
dependencies:
  # Stack Python (analyse, chiralité, graphiques)
  - python=3.11
  - numpy
  - scipy
  - matplotlib
  - pandas
  - h5py
  - jupyterlab
  - tqdm
  # Outils pour compiler DIVE (C++ / CGAL)
  - cmake
  - cxx-compiler
  - cgal-cpp
  - gmp
  - mpfr
  - libboost-devel
  - pip